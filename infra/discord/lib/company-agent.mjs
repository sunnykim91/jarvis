#!/usr/bin/env node
/**
 * Company Agent - Standup & Finance Runner
 * Stub implementation (미구현)
 */

const team = process.argv[2] || 'default';
const cmd = process.argv[3] || 'run';

console.log(`[company-agent] team=${team} cmd=${cmd} (stub - 아직 미구현)`);
process.exit(0);
