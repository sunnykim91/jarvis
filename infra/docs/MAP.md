# Jarvis MAP — AI Entry Point

> **Start here if you are an AI agent (or human) touching this repo for the first time.**
> 1-minute read. Tells you what Jarvis is, where things live, and which doc to open next.

> Existing sibling docs: [ARCHITECTURE.md](ARCHITECTURE.md) (message flow · runners) · [SYSTEM-OVERVIEW.md](SYSTEM-OVERVIEW.md) (auto-gen daily) · [OPERATIONS.md](OPERATIONS.md) (cron/incident) · [TASKS-INDEX.md](TASKS-INDEX.md) (auto-gen task catalog) · [CONFIG.md](CONFIG.md) · [TEAMS-CRONS.md](TEAMS-CRONS.md)

---

## 1. Purpose

`~/jarvis` is the **infra-heavy half** of the Jarvis ecosystem. It contains:

- ~90+ scheduled tasks (crons + event-triggered) — exact count in `tasks-index.json.totalTasks`
- A 24/7 Discord bot
- A RAG knowledge base (LanceDB + Ollama hybrid)
- `ask-claude.sh` — the LLM gateway used by every cron
- A virtual organization of ~11 AI teams that own cron subsets and Discord channels

Sister project: `~/jarvis-board` — a Next.js app that visualizes this ecosystem as a pixel-art virtual office ("자비스맵"). It is **read-only** from this repo; do not modify it here.

---

## 2. Top-level layout

```
~/jarvis/
├── infra/
│   ├── scripts/          # Shell + Node + Python cron/automation scripts
│   ├── lib/              # Shared helpers (llm-gateway.sh, task-store.mjs, …)
│   ├── discord/          # Discord bot + handlers
│   ├── docs/             # ← you are here
│   ├── agents/           # Agent profiles (Markdown personas)
│   ├── teams/            # Per-team config (team.yml, system.md, prompt.md)
│   ├── nexus/            # In-proc MCP bridge (mcp-nexus.mjs)
│   ├── prompts/          # Reusable prompt fragments
│   ├── templates/        # Templates for docs / plists / tasks
│   ├── watchdog/         # Bot watchdog + self-heal
│   ├── config/           # Repo-checked config (vs runtime at ~/.jarvis/config/)
│   ├── plugins/          # Plugin slots
│   ├── package.json      # infra Node deps
│   └── docker-compose.yml
├── rag/                  # RAG indexer (LanceDB + Ollama, hybrid BM25 + dense)
│   ├── bin/              # rag-cli entry points
│   ├── lib/              # indexer, query, schema
│   ├── scripts/          # rag-index.mjs, rag-compact.mjs, rag-bench
│   └── templates/
├── scripts/              # Top-level setup wizards (setup_rag.py, setup_infra.py)
├── private/              # Owner-private config (do NOT touch in refactors)
├── docs/                 # Repo-wide public docs (README images, etc.)
├── README.md / README.ko.md
└── CLAUDE.md             # Project-level Claude Code policy
```

Runtime state (not in repo):

```
~/.jarvis/                 # → symlink to ~/jarvis/runtime/ (A2 migration, 2026-04-17)
├── config/               # (real) Live config — tasks.json is SSoT (see CONFIG.md)
├── logs/                 # (real) All cron stdout/stderr + claude-stderr-<id>-*.log
├── data/                 # (real) RAG vector store, index state
│   ├── lancedb/          #        destroyed twice — handle with care
│   └── index-state.json
├── state/                # (real) sessions, rate-tracker, caches
├── ledger/               # (real) append-only JSONL audit trails
├── wiki/                 # (real) LLM Wiki pages per userId
├── private/              # (real) personal-only scripts — repo-ignored, your data
├── scripts/              # → symlink to ~/jarvis/infra/scripts/  (OSS code, replaced by updates)
├── bin/                  # → symlink to ~/jarvis/infra/bin/
├── lib/                  # → symlink to ~/jarvis/infra/lib/
└── infra/                # → symlink to ~/jarvis/infra/
```

**Why the split**: code (symlinks into `~/jarvis/`) and personal data (real dirs) share a root so crons reference stable paths, but `git pull` only replaces the symlinked code — your data is structurally untouchable. Personal helper scripts that shouldn't go public belong in `~/.jarvis/private/` (repo-ignored).

---

## 3. Key subsystems

### Cron scheduler

- **SSoT**: `~/jarvis/runtime/config/tasks.json` (task count in `tasks-index.json.totalTasks`)
- **Runner**: `infra/lib/task-store.mjs` + shell wrappers under `infra/scripts/`
- **Gateway**: Every LLM task goes through `infra/lib/llm-gateway.sh` → `ask-claude.sh`
- **Budgets**: `maxBudget` per task + `team-budget.json` per team cap + daily global cap
- **Reliability**: circuit breaker, retry with backoff, ledger (append-only JSONL), Tier 1-5 auto-tune

Full index: [TASKS-INDEX.md](TASKS-INDEX.md) (auto-generated by `gen-tasks-index.mjs`).

### Discord bot

- **Entry**: `infra/discord/discord-bot.js`
- **Runner**: `lib/claude-runner.js` → @anthropic-ai/claude-agent-sdk (1M context beta always on)
- **Process mgmt**: `ai.jarvis.discord-bot` LaunchAgent (long-running)
- **Handlers**: `infra/discord/lib/handlers.js` + `pre-processor.js`
- **PDF**: pdftotext → fallback to Claude Read
- **Streaming**: 1,900-char chunks to Discord
- **Full flow**: [ARCHITECTURE.md](ARCHITECTURE.md#message-flow)

### RAG knowledge base

- **Code**: `rag/` (indexer, query, schema)
- **Store**: `~/.jarvis/data/lancedb/` (LanceDB vector)
- **Embedding**: Ollama local (bge-m3 by default)
- **Query**: Hybrid BM25 + Dense + optional reranker
- **Live indexer**: `ai.jarvis.rag-watcher` LaunchAgent
- **Weekly compact**: `ai.jarvis.rag-compact` (Sun 03:00) — **must unload watcher first**
- **Destruction rule**: Never compact while watcher is loaded. See `~/.claude/rules/rag-system.md`

### LLM gateway (`ask-claude.sh`)

- All cron-driven LLM calls flow through this one script
- Uses `--mcp-config empty-mcp.json` (zero MCP) — only Claude Code CLI gets full MCP
- Reads `ANTHROPIC_API_KEY` or `CLAUDE_CODE_OAUTH_TOKEN`
- Emits token ledger rows to `~/.jarvis/logs/token-ledger.jsonl` for `token-ledger-audit`

### Virtual teams

11 AI team personas (`infra-lead`, `finance`, `trend-lead`, `record-lead`, `library`, `growth-lead`, `brand-lead`, `audit-lead`, `secretary`, `president`, `standup`). Defined in `~/jarvis-board/lib/map/team-registry.ts` (SSoT). Each team owns a subset of crons (see [TEAMS-CRONS.md](TEAMS-CRONS.md)) and posts to a specific Discord channel.

---

## 4. Where to find what

| I need to… | File / dir |
|---|---|
| Add a new scheduled task | `~/.jarvis/config/tasks.json` → run `gen-tasks-index.mjs` |
| Understand what cron X does | [TASKS-INDEX.md](TASKS-INDEX.md) → script/prompt under `infra/scripts/` |
| Find which team owns cron X | [TEAMS-CRONS.md](TEAMS-CRONS.md) or `infra/docs/tasks-index.json` |
| Edit config safely | [CONFIG.md](CONFIG.md) |
| Trace a Discord message flow | [ARCHITECTURE.md](ARCHITECTURE.md) |
| Check today's system state | [SYSTEM-OVERVIEW.md](SYSTEM-OVERVIEW.md) (auto-regenerated daily 04:05) |
| Investigate an incident | [OPERATIONS.md](OPERATIONS.md) + `~/.jarvis/logs/<task-id>.log` |
| Add a new AI team | Edit `~/jarvis-board/lib/map/team-registry.ts` (SSoT, in sister repo) + mirror keywords in `gen-tasks-index.mjs` |
| Test a cron locally | `node ~/.jarvis/scripts/<task-id>.mjs` or the `script` field's target |
| Rebuild RAG | Unload `ai.jarvis.rag-watcher` → backup `~/.jarvis/data/lancedb` → `rag-compact.mjs` → reload watcher |
| Find shell helpers | `infra/lib/*.sh` (llm-gateway, event-bus, log-utils, lint-gate) |
| Find Node helpers | `infra/lib/*.mjs` (task-store, task-fsm, mcp-nexus, message-queue) |
| Find agent personas | `infra/agents/*.md` |
| Read prompt fragments | `infra/prompts/` |

---

## 5. Entry points for AI agents

**Read in this order** (total < 2 minutes):

1. **`~/jarvis/CLAUDE.md`** — project-level policy (privacy, naming, emoji output rule)
2. **`~/jarvis/infra/docs/MAP.md`** — this file
3. **`~/jarvis/infra/docs/TASKS-INDEX.md`** — what exists, grouped by team
4. **Target-specific doc** — whichever of the following is closest to the task:
   - `TEAMS-CRONS.md` if user mentions a team
   - `CONFIG.md` if user is changing config
   - `ARCHITECTURE.md` if user is debugging the Discord bot or LLM runner
   - `OPERATIONS.md` if user is firefighting
   - `SYSTEM-OVERVIEW.md` for current runtime snapshot

Then read the specific script with Serena (`find_symbol`, `get_symbols_overview`) — **not** full-file `Read` for scripts > 500 lines.

---

## 6. Related projects

| Repo | Role | Status from here |
|---|---|---|
| `~/jarvis-board` | Next.js virtual office UI (자비스맵) + canonical `team-registry.ts` | **Read-only** |
| `~/.jarvis/` | Runtime state (config, logs, RAG DB, LanceDB, scripts) | Writable via crons / manual ops |
| `~/.claude/` | Claude Code global rules, hooks, skills, projects memory | Global, not project-specific |

> `~/jarvis-board` owns the team SSoT. When refactoring teams, edit there first, then sync the keyword mirror in `infra/scripts/gen-tasks-index.mjs`.

---

## 7. Don'ts

- Don't hand-edit `effective-tasks.json` (runtime cache; `cron-sync.sh` regenerates it)
- Don't compact RAG while `rag-watcher` is loaded — 2 prior DB destructions
- Don't hardcode user paths; use `BOT_HOME` / `JARVIS_HOME` / `JARVIS_RAG_HOME`
- Don't put prompts in `ask-claude.sh` with MCP enabled — gateway always passes `empty-mcp.json`
- Don't Read 1,500+ line files without trying Serena first (`find_symbol` / `get_symbols_overview`)
- Don't commit `.env`, `jira.env`, `tutoring.env`, `google-calendar-token.json`, or anything under `~/.jarvis/config/secrets/`
- Don't add a new team by creating a keyword list here — edit `~/jarvis-board/lib/map/team-registry.ts` first

